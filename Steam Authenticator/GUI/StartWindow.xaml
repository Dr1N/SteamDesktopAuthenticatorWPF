<Window x:Class="Authenticator.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Authenticator"
        mc:Ignorable="d"
        Title="Choise Action" 
        Height="200" 
        Width="200"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowActivated="True"
        Icon="/Resources/Images/icon.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0"
                Margin="10,5"
                Name="RunAuthButton"
                Content="Start Authenticator"
                Click="RunAuthButton_Click"/>
        <Button Grid.Row="1"
                Margin="10,5"
                Name="AddAuthButton"
                Content="Activate Authenticator" 
                Click="AddAuthButton_Click"/>
        <Button Grid.Row="2"
                Margin="10,5"
                Name="RemoveAuthButton"
                Content="Deactivate Authenticator" 
                Click="RemoveAuthButton_Click"/>
    </Grid>
</Window>
